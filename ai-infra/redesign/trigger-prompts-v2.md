# Trigger Prompts v2 — 新定时任务 prompt 全文

> 草稿，不应用。由 Cowork 按此改现有定时任务（trig_id 见 `notion-rules-v2.md` §6）。
> 每条 prompt 独立可执行，格式参照现有任务写法。

---

## 周日规划（周日 20:00，`trig_01Nn28LkoeVXWVewBbSB6AXe`）

```
读 Notion「AI-Infra 工作台」的「模块进度」database，找出当前进行中和下一个待开始的模块。

给出下周 3–5 个 session 的建议清单：每条标明对应模块、阶段（①-④）、预计时长。
不要预建 kata/study 条目，不要按星期几分配死——这只是建议清单，yijie 自己决定哪天做哪条。

只发一封邮件列出建议清单，并把每个提到的模块的「下一步」字段更新成一句话摘要
（例如"读 kv_cache_manager.py 的 allocate_slots，写 M1 的源码对照 note"）。

不使用"落后""未完成""疏远"等措辞。只陈述当前状态和建议，不做评判。
```

---

## 每日 brief（`trig_011YdiiqbXa57SB5EMLuP2JW`）—— 两个方案，待 yijie 选

### 方案 A：条件触发

```
读 Notion「模块进度」database。

仅当同时满足以下两个条件时才生成 brief：
1. 存在至少一个模块的「下一步」字段非空
2. 今天没有已经标记为"进行中"的 session

满足条件时：按该模块当前阶段（①-④）生成对应材料
（① minimal 任务+分级提示 / ② 源码阅读清单 / ③ worked example+复述题 / ④ PR 测验 brief），
发一封邮件。不满足条件时，不发送任何内容（静默跳过，不发"今天没有安排"这类空消息）。

不使用"落后""未完成""疏远"等措辞。
```

**取舍**：保留一点被动提醒兜底遗忘风险（长时间没碰某模块时还能被动提醒一下），但仍然依赖「下一步」字段被及时维护，否则会连续静默。

### 方案 B：取消

```
（无 prompt 正文——直接删除/永久暂停这个定时任务。）
```

**取舍**：彻底避免"每天生成欠账感"的旧问题根源，完全交给 yijie 主动发起 `/study`；风险是没有任何兜底提醒，如果「模块进度」长期没人看，不会有人知道。

---

## 周五 capstone（`trig_01TtFUzro5gbFspQ9dQqjyxu`）—— 取消

不写新 prompt。功能并入④阶段 PR 综合测验（见 `notion-rules-v2.md` §5.2/§5.3），不再单独作为每周固定节点存在。建议 Cowork 直接永久暂停/删除这个定时任务。

---

## 知识库体检（`trig_01UaLo7L3P4Brh8T8dGMSu2Z`）

```
读 Notion「模块进度」database 与掌握度 database，统计以下数字：
- 各方向 notes 数量、最近一次更新距今天数
- 「模块进度」里各模块所处阶段分布
- 掌握度 database 里复查日期已过期但未复查的条目数

只陈述数字，不做"疏远/降级/建议合并方向"这类判断性结论。
推送一封邮件列出上述数字，附一句话："以上仅供参考，如何安排由你自己决定。"
```

### 新旧措辞对照表

| 旧版本（要删除） | 新版本 |
|---|---|
| "超过 21 天没动就点名" | 删除这条判据，不做任何"点名" |
| "连续 3 次疏远会直接建议降级或合并该方向" | 删除这条判据 |
| "落后 / 未完成 / 疏远" 等推送文案 | 一律不出现，只陈述数字 |
| 覆盖写入 `STATUS.md` | 删除——`STATUS.md` 已废弃，体检结果只发邮件+更新 Notion 相关字段，不写本地文件 |
